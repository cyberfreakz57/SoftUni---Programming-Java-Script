function convert(input) {
    //take number from input
    let a = Number(input[0]);
    //convert inc to cm (1 inc = 2.54 cm)
    let answer = a * 2.54;
    //output
    console.log('Result:', answer);
}
convert(['5']) 

function convert2(input) {
    //take number from input
    let a = Number(input[0]);
    //convert inc to cm (1 inc = 2.54 cm)
    let answer = a * 2.54;
    //output
    console.log('Result:', answer);
}
convert2(['7']) 